require 'rails_helper'

=begin
RSpec.describe "TradingManager", :type => :request do
  describe "GET /widgets" do
    it "works! (now write some real specs)" do
      get widgets_path
      expect(response.status).to be(200)
    end
  end
end
=end
